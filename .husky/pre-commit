npx lint-staged

# Match CI checks exactly
npx eslint .
npx prettier --check '**/*.ts'
npx markdownlint-cli2
npx tsc --noEmit

npx tsx scripts/generate-po-index.ts
git add e2e/po/INDEX.md

# Validate the Playwright runner compose config
TEST_BASE_URL=dummy docker compose -f docker-compose.pw.yml config -q
